<div dir="rtl">

# حماية كود ALOKA Live من العميل

## الحقيقة التقنية أولاً (مهم جداً)

**لا يمكن «قفل مجلد بكلمة سر» بحيث لا يفتحه صاحب الاستضافة.** أي عميل يملك حساب الاستضافة
(cPanel / DirectAdmin / FTP / SSH / File Manager) يستطيع فتح وقراءة وتعديل وحذف **أي** ملف داخل
حسابه. لا يوجد «كلمة سر على مجلد» تمنع مالك الملفات نفسه — كلمة السر (Basic Auth عبر `.htaccess`)
تمنع فقط الزائر عبر المتصفح (HTTP)، ولا تمنع الوصول عبر مدير الملفات أو FTP.

لذلك، من يبيع «سكربت PHP لا يستطيع العميل قراءته أو تعديله» يستخدم في الحقيقة أحد أمرين:

1. **تشفير الكود المصدري (IonCube / SourceGuardian / Zend Guard)** — الحل الفعلي.
2. **عدم إعطاء العميل صلاحية كاملة على الملفات** (تستضيف أنت، أو تمنع الكتابة على `app/`).

---

## 1) الحل الحقيقي: تشفير الكود بـ IonCube (موصى به)

يحوّل ملفات PHP إلى صيغة مشفّرة لا تُقرأ ولا تُعدّل، ويعمل الموقع بشكل طبيعي عبر «محمّل» IonCube
على السيرفر. حتى لو فتح العميل الملف يرى رموزاً مشفّرة فقط، ولا يستطيع رؤية منطق الترخيص أو
`SIGN_KEY` أو تعطيل الفحص.

**الخطوات:**

1. احصل على مُشفّر IonCube (مدفوع) من: `https://www.ioncube.com/`.
2. شفّر على الأقل الملفات الحسّاسة (ويفضّل مجلد `app/` كاملاً):
   - `app/Core/License.php`  ← الأهم (منطق الترخيص + المفتاح)
   - `app/config.php`
   - `app/bootstrap.php`
   - `app/Controllers/Admin.php`
   - `public/api/_boot.php`
   مثال أمر التشفير:
   ```bash
   ioncube_encoder5 app/ -o app_encoded/ --replace-target-file
   ```
3. تأكد أن استضافة العميل عليها إضافة **ioncube loader** مفعّلة
   (أغلب الاستضافات تدعمها؛ تُفعّل من `php.ini` أو من لوحة الاستضافة).
4. وزّع النسخة المشفّرة للعميل. المنطق كله (بما فيه فحص الترخيص) يصبح غير قابل للقراءة أو التعديل.

> بديل مماثل: **SourceGuardian** (`https://www.sourceguardian.com/`) — نفس الفكرة، وأحياناً أرخص.

بهذا يصبح تعديل `License.php` أو `SIGN_KEY` أو تعطيل بوابة الترخيص **مستحيلاً عملياً** على العميل،
ويكتمل التحكم من Blogspot الذي بنيناه.

---

## 2) الحماية المضافة داخل المشروع (منفّذة بالفعل)

- **مجلد `app/` خارج جذر الويب**: جذر الموقع هو `public/` فقط، لذلك ملفات `app/` و`storage/`
  **غير قابلة للوصول عبر المتصفح إطلاقاً** (لا يمكن لأحد فتح `tofixtv/app/Core/License.php` من الرابط).
- **`app/.htaccess` و`storage/.htaccess`**: `Require all denied` — منع كامل لأي وصول HTTP لهذه
  المجلدات (طبقة دفاع إضافية لو أخطأ أحد بضبط جذر الموقع على مجلد المشروع بدل `public/`).
- **توقيع HMAC على `storage/settings/license.json`**: أي تعديل يدوي لحالة الترخيص يُكتشف ويُقفل
  الموقع فوراً (راجع `LICENSE_SETUP.md`).

هذه الطبقات تمنع وصول الزوّار والوصول عبر HTTP، وتمنع تلاعب العميل بملف حالة الترخيص. لكنها —
كأي كود PHP على سيرفر يملكه العميل — لا تمنع من لديه صلاحية كتابة كاملة على `app/` من تعديل الكود
نفسه. لذلك التشفير (البند 1) هو المكمّل.

---

## 3) قفل عبر كلمة سر (Basic Auth) — وحدوده

يمكنك وضع كلمة سر على مجلد عبر `.htaccess` + `.htpasswd`، لكن **افهم حدودها**:
تمنع فتح المجلد عبر **المتصفح (HTTP)** فقط — ولا تمنع العميل من فتح الملفات عبر مدير الملفات أو FTP،
ولا يجوز وضعها على `app/` أصلاً لأن ذلك سيمنع الموقع نفسه من تحميل ملفاته ويعطّله.

الاستخدام الصحيح الوحيد: حماية **مسار لوحة الإدارة** `/admin` بطبقة كلمة سر إضافية أمام صفحة الدخول
(اختياري). مثال داخل `public/.htaccess` أو إعداد السيرفر:

```apache
<If "%{REQUEST_URI} =~ m#^/admin#">
  AuthType Basic
  AuthName "Restricted"
  AuthUserFile /path/absolute/.htpasswd
  Require valid-user
</If>
```
(تُنشئ `.htpasswd` بأمر `htpasswd -c /path/.htpasswd admin`.)
هذا يضيف حاجز كلمة سر أمام لوحة الإدارة فقط، ولا علاقة له بحماية الكود المصدري.

---

## الخلاصة

| الهدف | الأداة الصحيحة |
|---|---|
| العميل لا يقرأ/يعدّل الكود | **تشفير IonCube / SourceGuardian** (البند 1) |
| لا أحد يفتح ملفات الكود عبر المتصفح | جذر الويب `public/` + `.htaccess` (منفّذ) |
| العميل لا يتلاعب بحالة الترخيص | توقيع HMAC (منفّذ في `License.php`) |
| التحكم بالإيقاف/التشغيل عن بُعد | صفحة Blogspot (منفّذ) |
| حماية قصوى بلا تشفير | لا تمنح العميل صلاحية كتابة على `app/`، أو استضِف أنت |

</div>
